name: 🚀 Deploy website on push
on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 45.132.157.8
          username: u262084135.blanchedalmond-grasshopper-461561.hostingersite.com
          password: Cr@zyoff1209
          server-dir: ./   # ou /public_html/ se esse for o diretório do site
          local-dir: ./
          dangerous-clean-slate: true
